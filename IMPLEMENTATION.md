# 改修内容

## 参照元との差分と方針

- `routemap-kana` の StrokeSVG 文字データ、座標変換、手書き入力、`evaluateTrace` 判定処理をそのまま再利用した。
- 判定設定は参照元と同じ「やさしい・ふつう・しっかり」。既定値も同じ「ふつう」。
- 旅程（3日・34地点）、乗り換え表示、訪問地写真、地点・区間の Google マップリンク、到着記録、JSON 入出力は旅行アプリ側の機能として維持した。
- 到着は `visited`、練習は地点ごとの文字位置配列 `practice` に分離した。保存版は `version: 2`。
- 旧キーと `arrived` / `done` / `practiced` / `practiceProgress` を読み込み時に移行する。

## 判定値（参照元と同一）

| 設定 | 軌跡からの許容距離 | 軌跡内の最低比率 | お手本被覆の最低比率 | 筆数・始点・画順を合否に使用 |
| --- | ---: | ---: | ---: | --- |
| やさしい | 112 | 0.42 | 0.28 | しない |
| ふつう | 92 | 0.54 | 0.38 | しない |
| しっかり | 70 | 0.68 | 0.52 | する |

共通値は、最低筆跡長 18、始点許容距離 82、方向一致率 0.58。入力とお手本は間隔 10 で再サンプリングする。

## 参照元から変更した点

参照元は誤答後に「このまま できた」で通常完了へ進められるため、判定に失敗した字も完了記録へ入る。今回の要件では誤字通過になるので、この上書き経路を削除した。失敗時は理由を表示し、「ひとつ もどす」または「けす」で直す。全ての文字位置が判定合格したときだけ地点の練習を完了とし、最終文字の完了画面では「もういちど かく」が先頭文字へ戻る。

手書き面は参照元の Pointer Events 実装を使用し、`touch-action: none`、pointer capture、画面外での `pointerup` / `pointercancel`、非 passive の `touchmove` 抑止を維持している。
